{
  "name": "google-sheets-addon-backend",
  "version": "1.0.0",
  "description": "",
  "main": "app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "deno run --allow-env --allow-read --allow-net app.ts"
  },
  "author": "railflow",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "ngrok": "^5.0.0-beta.2"
  }
}
